<Window x:Class="ThetaNetSample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ThetaNetSample"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="Button" x:Key="commandButtonStyle">
            <Setter Property="Margin" Value="5,10" />
            <Setter Property="MinWidth" Value="80" />
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <WrapPanel>
            <Button Content="Init" Style="{StaticResource commandButtonStyle}" x:Name="btnInit" Click="BtnInit_Click"></Button>
        </WrapPanel>
        <TextBlock Text="Result" Grid.Row="1" Margin="5,5,0,0" />
        <Border Grid.Row="2" BorderBrush="#888" BorderThickness="1" Margin="5">
            <TextBlock x:Name="txtOutput" />
        </Border>
    </Grid>
</Window>
